import { Server } from '../common/json-rpc/Server';
import { IServerOpts, ScriptingTransport } from '../common/json-rpc/types';
export declare class TransportBasedServer extends Server<ScriptingTransport> {
    transport: ScriptingTransport;
    constructor(transport: ScriptingTransport, opt?: IServerOpts);
    sendMessage(receiver: ScriptingTransport, message: string): void;
    getAllClients(): ScriptingTransport[];
}
